import { useState } from "react";
import axios from "axios";
import toast from "react-hot-toast";
import DashboardLayout from "../../components/dashboard/DashboardLayout";
const API="https://smart-society-backend-delta.vercel.app/visitor"; const config=()=>({headers:{Authorization:`Bearer ${localStorage.getItem("token")}`}});
export default function VisitorRequest(){const[flatNo,setFlatNo]=useState("");const[resident,setResident]=useState(null);const[form,setForm]=useState({purpose:"",visitorType:"Guest",visitDate:"",visitStartTime:"",visitEndTime:""});const find=async()=>{try{setResident((await axios.get(`${API}/residents?flatNo=${encodeURIComponent(flatNo)}`,config())).data.data)}catch(e){setResident(null);toast.error(e.response?.data?.message||"Resident not found")}};const submit=async e=>{e.preventDefault();try{const r=await axios.post(`${API}/requests`,{...form,residentId:resident?._id,visitStartTime:`${form.visitDate}T${form.visitStartTime}`,visitEndTime:`${form.visitDate}T${form.visitEndTime}`},config());toast.success(r.data.message)}catch(e){toast.error(e.response?.data?.message||"Could not send request")}};return <DashboardLayout role="visitor"><div className="max-w-3xl"><p className="text-[10px] font-bold uppercase tracking-[.18em] text-[#9b7740]">Visitor Portal</p><h1 className="mt-2 text-2xl font-extrabold text-[#32143b]">Request a visit</h1><section className="mt-6 border border-[#e2d9df] bg-white p-6"><label className="text-xs font-bold">Resident flat number</label><div className="mt-2 flex gap-2"><input value={flatNo} onChange={e=>setFlatNo(e.target.value)} className="h-11 flex-1 border px-3" placeholder="e.g. A-101"/><button onClick={find} type="button" className="bg-[#32143b] px-5 text-sm font-bold text-white">Find resident</button></div>{resident&&<div className={`mt-4 border p-4 text-sm ${resident.acceptingRequests?"border-green-200 bg-green-50":"border-red-200 bg-red-50"}`}><b>{resident.name}</b> · Flat {resident.flatNo}<p className="mt-1">{resident.availabilityMessage}</p></div>}<form onSubmit={submit} className="mt-5 grid gap-4"><input required placeholder="Purpose of visit" onChange={e=>setForm({...form,purpose:e.target.value})} className="h-11 border px-3"/><select onChange={e=>setForm({...form,visitorType:e.target.value})} className="h-11 border px-3"><option>Guest</option><option>Delivery</option><option>Cab</option><option>Vendor</option></select><input required type="date" onChange={e=>setForm({...form,visitDate:e.target.value})} className="h-11 border px-3"/><div className="grid grid-cols-2 gap-4"><input required type="time" onChange={e=>setForm({...form,visitStartTime:e.target.value})} className="h-11 border px-3"/><input required type="time" onChange={e=>setForm({...form,visitEndTime:e.target.value})} className="h-11 border px-3"/></div><button disabled={!resident?.acceptingRequests} className="h-11 bg-[#9b7740] text-sm font-bold text-white disabled:opacity-50">Send visit request</button></form></section></div></DashboardLayout>}
